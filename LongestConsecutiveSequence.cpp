#include <bits/stdc++.h>
using namespace std;

int longestConsecutive(vector<int> &nums)
{
    vector<int> seq;
    sort(nums.begin(), nums.end());

    for(int num : nums){
        if(nums[num-1] == nums[num] - 1){
            
        }
    }
}

int main()
{

    return 0;
}